diff --git a/roles/ceph-osd/tasks/scenarios/osd_directory.yml b/roles/ceph-osd/tasks/scenarios/osd_directory.yml
index b277b8e..171d569 100644
--- a/roles/ceph-osd/tasks/scenarios/osd_directory.yml
+++ b/roles/ceph-osd/tasks/scenarios/osd_directory.yml
@@ -2,40 +2,53 @@
 ## SCENARIO 4: USE A DIRECTORY INSTEAD OF A DISK FOR OSD
 
 # NOTE (leseb): we do not check the filesystem underneath the directory
-# so it is really up to you to configure this properly.
+# it is assumed that you have defined a "noauto" mountpoint in /etc/fstab
 # Declaring more than one directory on the same filesystem will confuse Ceph.
 - name: create osd directories
-  file:
-    path: "{{ item }}"
-    state: directory
-    owner: "{{ dir_owner }}"
-    group: "{{ dir_group }}"
-  with_items: osd_directories
+  mount: fstype=xfs src={{item.1}} name={{item.0}} opts=noauto,noatime passno=2 state=mounted
+  with_together: 
+  - osd_directories
+  - devices
+  when: not (fs_journal_devices is defined)
+
+# NOTE (leseb): we do not check the filesystem underneath the directory
+# it is assumed that you have defined a "noauto" mountpoint in /etc/fstab
+# Declaring more than one directory on the same filesystem will confuse Ceph.
+- name: create osd directories
+  mount: fstype=xfs src={{item.1}} name={{item.0}} opts=noauto,noatime,logdev={{item.1|regex_replace('cachelv','fs_journal')}} passno=2 state=mounted
+  when: fs_journal_devices is defined
+  with_together: 
+  - osd_directories
+  - devices
+
+- name: clear out whatever files were in the mountpoint before
+  shell: rm -rf {{item}}/*
+  with_items: "{{ osd_directories }}"
 
 # NOTE (leseb): the prepare process must be parallelized somehow...
 # if you have 64 disks with 4TB each, this will take a while
 # since Ansible will sequential process the loop
+
 - name: prepare OSD disk(s)
-  command: "ceph-disk prepare {{ item }}"
-  with_items: osd_directories
-  changed_when: false
-  when: osd_directory
+  command: "ceph-disk prepare --journal-dev --data-dir {{ item.0 }} {{ item.1 }}"
+  with_together:
+  - osd_directories
+  - raw_journal_devices
 
 - name: activate OSD(s)
   command: "ceph-disk activate {{ item }}"
   with_items: osd_directories
-  changed_when: false
-
-- name: start and add that the OSD service to the init sequence
-  service:
-    name: ceph
-    state: started
-    enabled: yes
-  when: ansible_service_mgr != "systemd"
-
-- name: start and add the OSD target to the systemd sequence
-  service:
-    name: ceph.target
-    state: started
-    enabled: yes
-  when: ansible_service_mgr == "systemd"
